name: flutter_hooks
description: A flutter implementation of React hooks. It adds a new kind of widget with enhanced code reuse.
homepage: https://github.com/rrousselGit/flutter_hooks
version: 0.18.2+1

environment:
  sdk: ">=2.13.0-0.0.pre.306 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.16
